#!/usr/expect -f
set timeout 10
set password "eCTyD*R.94zTba"
spawn ssh -o StrictHostKeyChecking=no root@103.74.92.72 "cd /root/avatar/backend && source venv/bin/activate && python3 -c 'import app.main; print(app.main.__file__)'"
expect {
    "*assword:*" {
        send "$password\r"
        exp_continue
    }
    eof
}
