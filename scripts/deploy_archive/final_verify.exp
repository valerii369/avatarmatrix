#!/usr/bin/expect -f
set timeout 10
set password "eCTyD*R.94zTba"
spawn ssh -o StrictHostKeyChecking=no root@103.74.92.72 "grep 'version=' /root/avatar/backend/app/main.py; curl -s http://localhost:8000/health"
expect {
    "*assword:*" {
        send "$password\r"
        exp_continue
    }
    eof
}
