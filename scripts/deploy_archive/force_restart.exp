#!/usr/bin/expect -f
set timeout 20
set password "eCTyD*R.94zTba"
spawn ssh -o StrictHostKeyChecking=no root@103.74.92.72 "pkill -9 gunicorn; systemctl restart avatar-backend; sleep 5; curl -s http://localhost:8000/health"
expect {
    "*assword:*" {
        send "$password\r"
        exp_continue
    }
    eof
}
