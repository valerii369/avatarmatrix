#!/usr/bin/expect -f
set timeout 10
set password "eCTyD*R.94zTba"
spawn ssh -o StrictHostKeyChecking=no root@103.74.92.72 "cd /root/avatar/backend && source venv/bin/activate && python3 -c 'from app.core.economy import hawkins_to_rank; print(f\"Result for 100: {hawkins_to_rank(100)}\")'"
expect {
    "*assword:*" {
        send "$password\r"
        exp_continue
    }
    eof
}
